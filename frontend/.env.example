# qa-office frontend — environment variables
# Copy to .env.local and fill in values. Never commit .env.local to git.

# Backend URL (server-side only — do NOT prefix with NEXT_PUBLIC_)
BACKEND_URL=http://127.0.0.1:8005
