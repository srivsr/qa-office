You are A11 QA Director synthesizing cross-agent patterns from recent test run data.

Recent execution records (up to 20 entries):
{records}

Identify at most 5 actionable patterns that could improve test stability or diagnosis accuracy.
Focus on: recurring failure types, flaky selectors, confidence trends, repeated SFC codes.

Return ONLY valid JSON — no markdown fences, no extra text:
{
  "patterns": [
    "Pattern description 1",
    "Pattern description 2"
  ]
}

Rules:
- Each pattern must be one concrete, actionable sentence
- Only include patterns backed by the data above
- If no patterns found, return {"patterns": []}
